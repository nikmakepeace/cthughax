#!/bin/sh
APP_CONTENTS=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd) || exit 1
PANEL="$APP_CONTENTS/Applications/CthughaX Panel.app/Contents/MacOS/cthugha-panel"
if [ ! -x "$PANEL" ]; then
  echo "cthugha-panel helper not found: $PANEL" >&2
  exit 127
fi
exec "$PANEL" "$@"
